# GUI和系统集成
pystray==0.19.0          # 系统托盘支持
Pillow==10.0.0           # 图像处理 (GUI图标、托盘图标)

# 定时和通知
schedule==1.2.0          # 定时任务调度

# 窗口管理功能 (Windows专用)
pywin32>=306             # Windows API支持 (窗口操作、显示器检测)
pynput>=1.7.6            # 全局热键监听